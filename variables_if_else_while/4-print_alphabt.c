#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lettr;

	for (lettr = 'a'; lettr <= 'z'; lettr++)
	{

		if (letter == 'e')

			;

		else if (lettr == 'q')

			;

		else
			putchar(lettr);

	}
	putchar('\n');
	return (0);
}
